import { Candidate } from "../../models/Candidate";

export class CandidateView {

    public showProfile(candidate: Candidate): void {
        
    }

    private buildProfile(candidate: Candidate): HTMLBodyElement {
        return new HTMLBodyElement();
    }
}